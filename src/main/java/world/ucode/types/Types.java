package world.ucode.types;

public class Types {
    public enum GameType {
        HARD,
        MIDDLE,
        EASY,
        DEFAULT,
    }

    public enum PetType {
        DOG,
        CAT,
        DEFAULT,
    }
}